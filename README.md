# CompilerExample
 rev inter

part one 

### Setup

Generate ninja build files and build:

   ```sh
   $ cmake . -B build -G Ninja 
   $ cmake --build build
   ```
   
To run examples:

   ```sh
   $ ./build/examples/example
   ```
